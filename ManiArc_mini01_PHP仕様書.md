# ManiArc mini01 PHP課題用 仕様書

## 1. 作るもの

**ManiArc mini01** は、卒業制作で検討している ManiArc の簡易版として、ユーザーが「何に、いつから、どんな形で夢中だったか」をフォームで宣言し、その内容を CSV に保存して、一覧画面で確認できる PHP アプリです。

今回の課題では、ManiArc 本体の構想のうち、最も小さい単位である **「熱量の宣言」** と **「アーカイブ表示」** だけを実装します。

```text
熱量を宣言する
  ↓
CSVに保存する
  ↓
アーカイブとして一覧表示する
```

---

## 2. 課題要件との対応

| 課題要件 | ManiArc mini01 での実装 |
|---|---|
| フォーム送信 | 熱量宣言フォームを作成する |
| PHPで受信 | write.php で POST データを受け取る |
| ファイル操作 | data.csv に追記保存する |
| 一覧表示 | read.php で CSV を読み込み、表で表示する |
| アンケート的要素 | ジャンル、対象、開始年、愛の形を入力・選択する |

---

## 3. アプリ名

**ManiArc mini01**

サブタイトル：

> あなたの熱量を、宣言しよう。

---

## 4. コンセプト

ManiArc mini01 は、「現場に行った回数」や「課金額」だけでは測れない熱量を記録するためのミニアプリです。

ユーザーは、自分が夢中になっている対象について、以下のような情報を登録します。課題版は一人で使う個人アーカイブとし、宣言ごとに氏名などの個人情報は保存しません。

- 何への熱量か
- 誰・何に夢中なのか
- いつから夢中なのか
- どんな形で愛してきたのか
- その熱量についてのひとこと

本体の ManiArc では、推し活・スポーツ・ゲーム・コレクティングなど、あらゆる「夢中」を蓄積・可視化する構想ですが、今回の PHP 課題では CSV を簡易データベースとして使い、フォーム送信とファイル操作の基礎を確認します。

---

## 5. ファイル構成

```text
php01/
  data/
    data.csv        ← 保存先CSV
  input.php         ← 熱量宣言フォーム
  write.php         ← CSVへの書き込み処理
  read.php          ← アーカイブ一覧表示
```

---

## 6. input.php：熱量宣言フォーム

### 6-1. 役割

ユーザーが自分の熱量を宣言する画面です。

ManiArc 本体の「宣言」フェーズを、PHP 課題用に簡略化したものです。

---

### 6-2. 入力項目

| 項目 | name属性 | 種類 | 必須 | 内容 |
|---|---|---|---|---|
| ジャンル | category | select | 必須 | 推し活、スポーツ、ゲームなど |
| 対象名 | target_name | text | 必須 | 推し、チーム、作品、ゲーム名など |
| いつから夢中か | start_year | number | 必須 | 例：2018 |
| 愛の形 | love_types[] | checkbox | 必須 | 複数選択可 |
| ひとこと | comment | textarea | 任意 | その熱量についてのコメント |

※今回は氏名、Emailなどの個人情報は扱いません。

---

### 6-3. ジャンル選択肢

```text
推し活
スポーツ観戦・応援
コレクティング
ゲーム・eスポーツ
映像作品
食・ワイン
その他
```

---

### 6-4. 愛の形の選択肢

```text
辿る
考察する
体験する
集める・残す
没入する
語る・伝える
支える・応援する
```

複数選択できるようにします。

例：

```text
考察する / 没入する / 語る・伝える
```

---

### 6-5. 画面イメージ

```text
ManiArc mini01

あなたの熱量を、宣言しよう。

01. 何への熱量ですか？
[ 推し活 ▼ ]

02. 誰のことが、あなたをここに連れてきましたか？
[ BTS ]

03. いつから夢中でしたか？
[ 2024 ]

04. どんな形で愛してきましたか？
□ 辿る
□ 考察する
□ 体験する
□ 集める・残す
□ 没入する
□ 語る・伝える
□ 支える・応援する

ひとこと：
[ ライブでの記憶が自分の支えになっている ]

[ 宣言する ]
```

---

## 7. write.php：CSV保存処理

### 7-1. 役割

input.php から送信された POST データを受け取り、`data/data.csv` に 1 行追記します。

---

### 7-2. 保存するデータ

CSV には以下の順番で保存します。

```text
登録日時,
ジャンル,
対象名,
開始年,
継続年数,
愛の形,
コメント
```

---

### 7-3. 継続年数の計算

`start_year` から現在年を引いて、継続年数を計算します。

例：

```text
現在年：2026
開始年：2024
継続年数：2年
```

PHP では以下のような考え方で計算します。

```php
$current_year = date('Y');
$years = $current_year - $start_year;
```

---

### 7-4. CSV保存例

```csv
2026-06-20 15:30:00,推し活,BTS,2024,2,体験する / 語る・伝える,ライブでの記憶が自分の支えになっている
```

---

### 7-5. 保存後の表示

保存完了後、以下を表示します。

```text
あなたの熱量を記録しました。

「あなたの2年間は、ここにある。」

[ アーカイブを見る ]
[ もう一度宣言する ]
```

---

## 8. read.php：アーカイブ一覧画面

### 8-1. 役割

CSV を読み込み、保存された熱量宣言を一覧表示します。

ManiArc 本体の「アーカイブ」を、PHP 課題用に表形式で再現します。

---

### 8-2. 表示項目

| 表示項目 | 内容 |
|---|---|
| 登録日時 | CSVに保存した日時 |
| ジャンル | 推し活、スポーツなど |
| 対象名 | BTS、ブルーロック、ワインなど |
| 開始年 | いつから夢中か |
| 継続年数 | 何年分の熱量か |
| 愛の形 | 選択した7軸 |
| ひとこと | コメント |

---

### 8-3. 画面イメージ

```text
ManiArc mini01 アーカイブ

これまでに宣言された熱量：3件

各宣言を表ではなく「熱量パスポート」のカードとして表示します。カードにはジャンル、対象、開始年、継続年数、愛の形、ひとこと、宣言日を表示します。
```

---

## 9. CSV設計

### data.csv

| No | カラム名 | 内容 |
|---|---|---|
| 1 | created_at | 登録日時 |
| 2 | category | ジャンル |
| 3 | target_name | 対象名 |
| 4 | start_year | 開始年 |
| 5 | years | 継続年数 |
| 6 | love_types | 愛の形 |
| 7 | comment | コメント |

---

## 10. バリデーション

### 10-1. 必須チェック

write.php で以下を確認します。

| 項目 | チェック内容 |
|---|---|
| ジャンル | 空でない |
| 対象名 | 空でない |
| 開始年 | 空でない |
| 愛の形 | 1つ以上選択されている |

---

### 10-2. 余裕があれば追加するチェック

| 項目 | チェック内容 |
|---|---|
| 開始年 | 数字かどうか |
| 開始年 | 現在年より未来ではないか |
| コメント | 長すぎないか |

---

## 11. セキュリティ対応

read.php で CSV の内容を HTML 表示するときは、必ず `htmlspecialchars()` を使います。

理由は、ユーザーが入力した文字列をそのまま HTML に出すと、悪意ある HTML や JavaScript が実行される可能性があるためです。

```php
htmlspecialchars($value, ENT_QUOTES, 'UTF-8')
```

---

## 12. デザイン方針

ManiArc 本体の思想に合わせて、以下を意識します。

| 方針 | 実装への反映 |
|---|---|
| 静かに蓄積する | 派手なSNS風UIにしない |
| 比較させない | ランキング表示をしない |
| いいねを置かない | read.phpには評価ボタンを置かない |
| 自分のアーカイブ | 一覧画面を「投稿一覧」ではなく「アーカイブ」と呼ぶ |
| 熱量の多様性 | 7軸を複数選択できるようにする |

---

## 13. 今回は実装しないこと

PHP 課題の範囲を超えるため、以下は実装しません。

| 機能 | 理由 |
|---|---|
| ログイン | CSV課題では重い |
| SMS認証 | 本体MVPでは構想にあるが、今回は不要 |
| DB保存 | 今回はCSV保存が課題 |
| 画像生成 | PHP基礎課題としては重い |
| ブロックチェーン | post-PMF構想であり、今回の宿題範囲外 |
| ZKP証明 | post-PMF構想であり、今回の宿題範囲外 |
| SNS共有 | 初期実装では優先しない |

---

## 14. 実装優先度

### 14-1. 必須

```text
input.php
write.php
read.php
data/data.csv
```

最低限、以下が動けば提出可能です。

1. フォームに入力する
2. 送信する
3. CSV に保存される
4. read.php で一覧表示される

---

### 14-2. 余裕があれば

```text
・継続年数を自動計算する
・愛の形を複数選択にする
・一覧を新しい順にする
・件数を表示する
・CSSでパスポート風にする
・保存完了画面に「あなたの◯年間は、ここにある。」と表示する
```

---

## 15. 提出時の説明文

> 卒業制作で検討している ManiArc の簡易版として、「ManiArc mini01」を作成しました。  
> ManiArc は、推し活・スポーツ・ゲームなど、何かに夢中でいた時間を記録し、自分だけの熱量の軌跡として蓄積するサービスです。  
> 今回の PHP 課題では、その中核機能である「宣言」と「アーカイブ」を最小構成で実装しました。  
> ユーザーはフォームから、ジャンル、対象名、いつから夢中だったか、どんな形で愛してきたかを入力します。送信された内容は PHP で受け取り、CSV に保存され、一覧画面でアーカイブとして確認できます。  
> データベースは使わず、CSV を簡易データベースとして扱うことで、フォーム送信、ファイル書き込み、ファイル読み込み、一覧表示の基本処理を学ぶ構成にしています。

---

## 16. 開発メモ

実装時は、まず以下の順番で進めます。

1. `input.php` でフォームを作る
2. `write.php` で `$_POST` の中身を `var_dump()` して確認する
3. `write.php` で CSV に保存する
4. `read.php` で CSV を読み込んで表示する
5. `htmlspecialchars()` を入れて安全に表示する
6. 見た目を整える
